<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Add LEGO Set</title>
  <link rel="stylesheet" href="/css/main.css" />
</head>

<body class="bg-gray-100">
  <%- include('partials/navbar', { page: '/lego/addSet' }) %>
  <div class="container mx-auto p-4">
    <div class="bg-white rounded-lg shadow p-6">
      <div class="text-center">
        <h1 class="text-3xl font-semibold text-gray-800">Add LEGO Set</h1>
        <p class="text-gray-600 mt-2">Enter the details of the new LEGO set.</p>
      </div>
      <!-- Form -->
      <form action="/lego/addSet" method="POST" class="mt-4">
        
        <div class="mb-4">
          <label for="name" class="block text-gray-700 font-semibold">Name:</label>
          <input type="text" id="name" name="name" required
            class="border rounded-lg py-2 px-3 w-full focus:outline-none focus:ring focus:border-blue-300">
        </div>

        <div class="mb-4">
          <label for="year" class="block text-gray-700 font-semibold">Year:</label>
          <input type="number" id="year" name="year" required
            class="border rounded-lg py-2 px-3 w-full focus:outline-none focus:ring focus:border-blue-300">
        </div>

        <div class="mb-4">
          <label for="num_parts" class="block text-gray-700 font-semibold">Number of Parts:</label>
          <input type="number" id="num_parts" name="num_parts" required
            class="border rounded-lg py-2 px-3 w-full focus:outline-none focus:ring focus:border-blue-300">
        </div>

        <div class="mb-4">
          <label for="img_url" class="block text-gray-700 font-semibold">Image URL:</label>
          <input type="url" id="img_url" name="img_url" required
            class="border rounded-lg py-2 px-3 w-full focus:outline-none focus:ring focus:border-blue-300">
        </div>

        <div class="mb-4">
          <label for="theme_id" class="block text-gray-700 font-semibold">Theme:</label>
          <select id="theme_id" name="theme_id" required
            class="border rounded-lg py-2 px-3 w-full focus:outline-none focus:ring focus:border-blue-300">
            <% themes.forEach(theme => { %>
              <option value="<%= theme.id %>"><%= theme.name %></option>
            <% }) %>
          </select>
        </div>

        <div class="mb-4">
          <label for="set_num" class="block text-gray-700 font-semibold">Set Number:</label>
          <input type="text" id="set_num" name="set_num" required
            class="border rounded-lg py-2 px-3 w-full focus:outline-none focus:ring focus:border-blue-300">
        </div>
        <!-- button -->
        <button type="submit"
          class="bg-blue-500 text-white py-2 px-4 rounded-lg hover:bg-blue-600 transition duration-300 ease-in-out">
          Add Set
        </button>
      </form>
    </div>

  </div>

</body>

</html>
